---
description: Conventions for Sifflet Monitors as Code YAML (workspace.yaml and monitor files)
globs:
  - "**/workspace.yaml"
  - "**/monitors/**/*.yaml"
  - "**/monitors/**/*.yml"
alwaysApply: false
---

# Monitors as Code YAML

When editing Monitors as Code YAML, apply the **sifflet-quality-as-code** skill: Monitor v2 format, schema-valid YAML, stable dataset identifiers, and `sifflet code workspace plan` before any destructive changes.
